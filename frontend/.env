# Local frontend environment variables
# DO NOT store secrets (API keys, admin secrets) in this file if it is committed.
# Use Vercel/your host to set production env vars instead.
# For local development, set the backend API base URL below.
VITE_API_BASE=http://localhost:5050
# For local admin actions (DELETE / PATCH competitions). Set to match backend ADMIN_SECRET.
# Do NOT commit this value to source control if it contains secrets.
# Example populated from local backend .env for convenience during development
VITE_ADMIN_SECRET="c1771b2a98bb04cfac7c5f713824fdd6ae5947676587c59e"